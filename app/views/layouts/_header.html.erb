<div class="header">
   <div class="left-side">
   <%# <%= image_tag "pet.png", :class => 'img-header'%> 
   <%= link_to posts_path do %>
    <%= content_tag :h2, "PET LOVERS" %> 
    <%end %>
   </div>
   <div class="search-bar">
    <%= form_tag posts_path, method: "GET" do %>
        <%= text_field_tag :query, params[:query], placeholder: "Search posts!"%>

    <%end%>
   </div>
   <div class="right-side">
       <%= content_tag :div, link_to("Home", posts_path) %>
       <%= content_tag :div, link_to("Log in", new_user_session_path)%>
   </div>
</div>